# Pre-armed smoke stopped before HTTP; coaching paused

**The release remains incomplete. Coaching is paused at generation 5.** The
signed-in browser restored access and the approved one-shot operator started.
The resume from generation 3 to 4 committed. An immediate browser assertion
mistook the intermediate query-rendering state for a missing result. The operator
never received its fresh-open signal and sent no create or accept request.

Root later read the committed generation-4 result and immediately submitted the
prepared re-pause once. It committed at **02:36:13.270170 UTC on September 25**.
An independent read verified paused generation 5 at 02:36:25.118853 UTC; the full
owner reconciliation confirmed it again at 02:37:07.188415 UTC. These times are
September 24 evening in America/Chicago.

The approved 60/90-second containment limits were **not demonstrated**. No
independent open readback was captured, and the failed assertion returned control
before containment. Context compaction then delayed recovery. Do not describe
this attempt as a successful bounded open interval. Real athletes could write
while the gate was open; the reconciliation below does not prove that no other
athlete wrote during that interval.

## Evidence

- Fresh platform preflight at 02:29:13 UTC matched the pinned deployment, three
  aliases and all 31 bindings. Checkpoint `c4a8ed8` CI `36006358927` passed.
- Both exact migration ledger identities and all 14 functions/16 triggers
  matched before opening. No schema, deployment or policy changed in this turn.
- The helper saved its exclusive attempt at 02:32:21.522 UTC and finished private
  preparation before READY. It exited with `prepared-readiness-wait-expired` at
  02:37:21.967 UTC. The fresh-open signal and all create/accept response/success
  artifacts are absent. Authenticated evidence plus source order proves no HTTP
  dispatch by this helper. Its marker and failure are retained; do not relaunch it.
- All 66 synthetic-owner scopes (54 public, 12 auth) have unchanged counts. The
  profile and revision-zero rows remain; there are no coaching output rows.
- Both original fixed plan identities and content hashes match the encrypted
  pre-revision baseline. This comparison is distinct from simply comparing two
  current queries. No replay or final resume was performed.
- Raw owner evidence is authenticated and encrypted in the existing private
  recovery directory. The [sanitized receipt](production-prearmed-result-2026-09-25.json)
  contains no credentials or synthetic owner identifiers.

## Next permitted work

Prepare and independently review a single-invocation browser controller that
waits for completed SQL results and enters containment on every error before
returning. Start its deadline before submitting resume. Preserve exact generation
checks, freshness limits, one-shot markers and the original smoke/replay gates.
Qualify the concrete adapter while paused using read-only queries before seeking
a new production opening.

The previous packet authorized one generation-3-to-4 opening. That authorization
has been used, even though no application HTTP was sent. Another opening needs
fresh approval for the corrected method. Do not rerun the original operator,
publish its late signal, resume on timeout, roll back to the incompatible old
application, activate numerical policy, or provision paid resources.
