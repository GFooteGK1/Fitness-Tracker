# W8 independent application review

Reviewer: W1/W4/W6/legacy-guard author, independent of W8 UI/client author. Scope: current NextActionCard, recommendation client, capture uncertainty, capture/recovery refresh call sites, origin propagation and parent Coach-context integration. Browser visual clearance remains pending the final W8 browser run and actual screenshot inspection.

Status: **application source and focused verification clear**. Browser visual clearance remains separate and pending the final W8 screenshot inspection.

| Finding | Evidence and consequence | Status |
| --- | --- | --- |
| W8-R1: date-only evidence shifted to prior day | `prescribed_sessions` sources use local `YYYY-MM-DD`; formatting as a UTC Date showed prior evening west of UTC | Fixed: date-only values remain date-only; actual table names receive friendly labels |
| W8-R2: tab-loss uncertainty had no recovery identity | Durable localStorage unknown marker outlived sessionStorage request envelope, leaving Today withheld with no lookup/retry entry | Resolved: durable minimal create request ID, exact frozen correction envelope, owner-only restoration; independently verified |
| W8-R3: same-owner cross-tab proof collision | Marker keyed by owner+surface URL allowed a different tab's request B confirmation to mark unresolved request A confirmed; could surface guidance while A remains uncertain | Resolved: proof binds exact request ID; recovery retains multiple owner+surface requests; alternate correction URL/key replays exact original body. Independent same-surface create/correction regressions pass |
| W8-R4: duplicate initial load | Mount invoked load directly and through uncertainty resolution, replacing the initial loaded action in the new fake-clock midnight test | Resolved: redundant mount load removed; independent midnight/expiry/owner races pass |

Initial independent focused command: `npx vitest run test/components/next-action-card.test.tsx test/components/next-action-races.test.tsx test/client/recommendations.test.ts test/client/capture-uncertainty.test.ts test/client/logging-request.test.ts test/recommendations/coach-context.test.ts --reporter=dot`. Result before the repairs above: 42 passed, 1 failed (the duplicate-load midnight scaffold interaction). No authority or phantom-capture failure in that run. Do not substitute this earlier run for final evidence.

Readback confirms: account-keyed component teardown and late-response owner checks; one visible persisted recommendation; expiration/timezone and local-day checks; idempotent shown/response identity; recovered earlier responses remain separate from current response; Done only records a response; Adjust preserves a separate destination; source-invalidated followup becomes unknown while prior details remain inspectable; coverage is explicit with a source revision fence; plan/log links survive outage. Post-save refresh runs separately from capture success. Origins are frozen in first text/photo/offline payload and v2 completion; v1 completion keeps its exact legacy contract. Parent Coach bridge revalidates persisted ID/reasons outside passive caching, with correct raw timezone conversion.

Final independent command added `test/personalized-coaching/engineering-scenarios.test.ts` and `test/database/engineering-lifecycle.test.ts` to the six-file list above, with `--maxWorkers 4`. Result: **8 suites, 106 passed, 4 explicit frozen-fixture decision skips**. Of these, W8/Coach focused suites are 46 passing tests; the main development harness is 56 passing decisions plus 4 unvalidated RPE/range decision labels, and the separate actual SQL lifecycle suite is 4 passing cases. TypeScript and scoped ESLint pass. The four development skips are documented in `w9-verification-plan.md`; they do not count as W8 failures or frozen decision acceptance.

Final source readback checked alternate corrections carry `edit.key` only within the original owner+URL recovery prefix, use the stored body/request ID, and mark certainty before removing the envelope. Another request's proof cannot release it. Actual SQL helper now uses the same request-bound proof format and verifies original-request recovery after both precommit and postcommit response loss.

Rendered readback: author reports 11 passing W8 browser cases; this reviewer independently opened `output/playwright/personalized-coaching/next-action-dark-390.png`. The mobile dark view has legible factual/estimated nutrition wording, clear response separation, plan/log links, and no horizontal clipping. The fixed bottom navigation remains present in the full-page screenshot. This single screenshot inspection does not replace the author's interaction and multi-size/theme cases. W9 broad/concurrency/journey acceptance remains the coordinator's responsibility. No live account/provider/production behavior is claimed.
