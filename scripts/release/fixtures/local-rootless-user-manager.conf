# Only podman-sociusfit-local, user@1000.service. No global user template change.
# Avoid the shared WSL user.slice path before the manager's first boot allocation.
[Service]
Slice=sociusfitlocal.slice
